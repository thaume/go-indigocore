{
  "name": "{{input `name`}}",
  "version": "x.x.x",
  "description": "{{input `description`}}",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{input `author`}}",
  "license": "{{input `license`}}"
}
